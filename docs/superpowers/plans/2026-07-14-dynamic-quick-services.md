# Finder 服务快捷动作执行计划

1. 为快捷动作策略补充排序、去重、上限、安全过滤测试。
2. 为面板“全部/常用/分类”分区补充去重和顺序测试。
3. 为动态 `.service` 清单和原子存储补充结构与幂等测试。
4. 实现核心策略、面板分区模型和标准服务 bundle 编码器。
5. 实现微型转发 helper 与 Host 专属动态服务管理器，接入启动与配置变更防抖刷新。
6. 扩展服务提供者，支持直达动作并执行实时授权与目标二次校验。
7. 更新面板分类、设置/诊断文案、卸载脚本、Cask 和中英文 README。
8. 运行 `git diff --check`、结构测试、Swift 测试、完整构建和签名检查。
9. 安装测试包，刷新系统服务，验证动态条目、完整面板、动作入队和卸载清理。
10. 深度审查差异后，按 `xxx(xxx): 中文xxx` 提交并推送，等待远程 CI 通过。
